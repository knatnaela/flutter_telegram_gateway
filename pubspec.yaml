name: flutter_telegram_gateway
description: "A Flutter package for easy interaction with the Telegram Gateway API."
version: 0.0.2
homepage: "https://github.com/knatnaela/flutter_telegram_gateway"

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
